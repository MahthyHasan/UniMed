import React from 'react';
import Layout from '../../layout/Layout'

function Home(props) {
    return (
        <Layout>
            This is home page
        </Layout>
    );
}

export default Home;