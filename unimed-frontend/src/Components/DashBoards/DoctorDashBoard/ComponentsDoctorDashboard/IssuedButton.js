import React from "react";

const IssuedButton = () => {
  return (
    <button
      style={{
        backgroundColor: "#6BCB77",
        color: "#ffffff",
        border: "none",
        padding: "10px 20px",
        fontSize: "16px",
        borderRadius: "8px",
        cursor: "pointer",
      }}
    >
      Issue Record
    </button>
  );
};

export default IssuedButton;
